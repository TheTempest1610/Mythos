<Control xmlns="https://spacestation14.io"
         xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls">
    <controls:StripeBack HasBottomEdge="False" StyleIdentifier="mythosOptionsActionBar">
        <BoxContainer Orientation="Horizontal" Align="End" Margin="2">
            <Button Name="DefaultButton"
                    StyleIdentifier="mythosOptionsActionButton"
                    Text="{Loc 'ui-options-default'}"
                    TextAlign="Center"
                    Margin="8 0" />

            <Button Name="ResetButton"
                    StyleIdentifier="mythosOptionsDangerButton"
                    Text="{Loc 'ui-options-reset-all'}"
                    StyleClasses="negative" />
            <Button Name="ApplyButton"
                    StyleIdentifier="mythosOptionsActionButton"
                    Text="{Loc 'ui-options-apply'}"
                    StyleClasses="OpenLeft" />
        </BoxContainer>
    </controls:StripeBack>
</Control>
